import styled from '@emotion/styled';
import {
  space,
  color,
  layout,
  flexbox,
  position,
  typography,
  border,
  background,
  shadow,
} from 'styled-system';

export const Image = styled.img`
  ${space}
  ${color}
  ${layout}
  ${flexbox}
  ${position}
  ${typography}
  ${border}
  ${background}
  ${shadow}
`